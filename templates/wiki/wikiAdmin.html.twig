{% extends 'base.html.twig' %}

{% block body %}
    <section class="Dashboard">
        <div class="titreDash">
            <h1>
                Admin Wiki
            </h1>
        </div>
        <table class="table table_striped">

            <thead class="tableau">
            <th> Id</th>
            <th> Title</th>
            <th> Date</th>
            <th> User</th>
            <th> Categorie</th>
            <th> ImageName</th>
            <th> MAJ </th>
            <th> Éditer/Supprimer</th>
            </thead>
            <tbody>
            {% for wiki in wikis %}
                <tr class="StyleDash">
                    <td>
                        {{ wiki.id}}
                    </td>
                    <td>
                        {{ wiki.titre}}
                    </td>
                    <td>
                        {{ wiki.date }}
                    </td>
                    <td>
                        {{ wiki.user }}
                    </td>
                    {% for categorie in wiki.categories %}
                        <td>
                            {{ categorie }}
                        </td>
                    {% endfor %}
                    <td>
                        {{ wiki.imageName }}
                    </td>
                    <td>
                        {{ wiki.updatedAt|date('d-m-Y') }}
                    </td>
                    <td class="crud">
{#                        <a class="edit" href="{{ path('wiki_edit', {id: wiki.id }) }}">#}
{#                            <i class="fa-solid fa-pen text-primary"></i>#}
{#                        </a>#}
                        <a class="delete" href="{{ path('wiki_delete', {id: wiki.id}) }}">
                            <i class="fa fa-trash text-danger"></i>
                        </a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </section>
{% endblock %}