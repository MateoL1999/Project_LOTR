{% extends 'base.html.twig' %}

{% block body %}
    <section class="section__commentEdit">
        <div class="Comment__h1">
            <h1 class="Titre__commentEdit">
                Modification de commentaire
            </h1>
        </div>

        {{ form_start(form) }}
        <div class="conteneurComment">
            <div class="Edit--commentTitre">
                {{ form_row(form.titre) }}
            </div>
            <div class="Edit--commentContenu">
                {{ form_row(form.contenu) }}
            </div>
        </div>
        <div class="submit--Insc">
            {{ form_row(form.save) }}
        </div>
        {{ form_end(form) }}
    </section>

{% endblock body %}